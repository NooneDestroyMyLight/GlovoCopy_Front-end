import { DEFAULT_LABEL } from "../../../utils/funTextIncert";

export const TEMPLATE_UA_MW_STORE_DELIVERY_FEE = {
  text_title:
    `Замовте на ${DEFAULT_LABEL}, щоб заощадити ${DEFAULT_LABEL} на зборах!` as string,
  text_order_cost_break_wall: "Вартість замовлення" as string,
  fee: `До ${DEFAULT_LABEL}1` as string,
  text_fee: "Доплата" as string,
  //
  text_terms:
    "Ми не маємо мінімальної суми замовлення. Натомість ми стягуємо невелику доплату з клієнтів, які роблять замовлення на суму меншу за встановлену." as string,
  text_button: "Зрозуміло" as string,
};
